import React from "react";
import Http from "../util/Http";

interface params {
  q: string;
  sources: string;
}

const useHttp = () => {
  return <div>useHttp</div>;
};

export default useHttp;
